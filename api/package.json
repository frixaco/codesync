{
	"name": "codesync-api",
	"private": "true",
	"scripts": {
		"build": "pnpm db:gen && tsc --project tsconfig.json",
		"db:gen": "prisma generate",
		"db:push": "prisma db push",
		"dev": "pnpm db:gen && tsnd --respawn --exit-child --files src/index.ts",
		"lint": "eslint .",
		"prettier": "prettier --write --loglevel warn ."
	},
	"dependencies": {
		"@fastify/auth": "^3.0.2",
		"@fastify/cookie": "^7.4.0",
		"@fastify/cors": "^8.1.0",
		"@fastify/oauth2": "^6.0.0",
		"@prisma/client": "^4.1.1",
		"dotenv": "^16.0.1",
		"fastify": "^4.3.0",
		"fastify-plugin": "^4.0.0",
		"got": "11.x"
	},
	"devDependencies": {
		"pino-pretty": "^8.1.0",
		"prisma": "^4.1.1"
	}
}
